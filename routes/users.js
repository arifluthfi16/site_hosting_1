var express = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/', function(req, res, next) {


  //Checking the ip is registered or  not in db

  //Login to view if the ip is registered

  //Direct to register page if the ip does not registered
});

module.exports = router;
